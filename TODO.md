# TODO

- TSA/FSA records are now SQLite-backed (`processed/tsa.db`/`processed/fsa.db`, `SqliteRecordStore` in `src/services/sqlite_store.py`) instead of resident `pd.DataFrame`s - `EstimationService` fetches only the rows matching a retrieved neighbor set per request, so the full tables aren't duplicated across every worker process. `lf.pkl` stays a full in-memory read (`datasets/<name>/interim/`), since it's only needed once at startup to fit the scaler and populate Qdrant, then goes out of scope - not a steady-state duplication problem. Analyst-facing `tsa.pkl`/`fsa.pkl` pickles are still written alongside the SQLite tables in `interim/` for notebook/bulk use.
- Topology columns are now also written as a plain JSON list (`processed/topology_cols.json`, via `scripts/_common.py::write_json_list()`) - the format `EstimationService` reads. The joblib copy (`interim/topology_cols.joblib`) stays for analyst/notebook use, same as the `tsa.pkl`/`fsa.pkl` pattern above.
- `datasets/eles/2026-06` is ingested (LF/TSA/FSA/topology_cols working, wired into `_make_scaler_for_dataset`); run further analysis on it as needed.
- FSA (frequency stability) is parsed for both `eles/2026-01` and `eles/2026-06` (`fsa.pkl` in each `interim/`), wired into `EstimationService` (`EstimationService.fsa`, `estimate_by_observed_generator()`/`estimate_by_failed_generator()`, both grouped by `failed_gen`/`measured_gen` just nested in opposite order), and exposed via `POST /api/v1/estimate/fsa/by-observed-generator` (primary) and `POST /api/v1/estimate/fsa/by-failed-generator` (both return HTTP 501 for datasets without FSA, e.g. `bus39`/`interscada/*`). The former `/estimate/by-generator`/`/estimate/by-location` routes were renamed to `/estimate/tsa/by-generator`/`/estimate/tsa/by-location` for namespace consistency.
- SSSA (small-signal stability) is now **parsed and wired into `EstimationService`** for both `eles/2026-01` and `eles/2026-06`. Parsing produces one merged `sssa.pkl`/`sssa.db` per dataset (one row per `state, mode_id, generator`: `real_part`/`imag_part` plus `ObsMag`/`ObsAng`/`ParMag`/`ParAng`/`ConMag`/`ConAng`, optionally split per generator state variable) - `sssa_modes`/`sssa_participation` are joined once at parse time (`real_part`/`imag_part` are mode-level values that legitimately repeat across every generator row sharing a mode) rather than per query. `mode_id` is a per-state local identifier only, never comparable across states (see the SSSA sections of `datasets/eles/*/README.md`) - `EstimationService.estimate_sssa_by_generator()` groups by `generator` only (the one identity that *is* comparable across states) and never by `mode_id`. This is deliberately a **raw, unweighted retrieval** - no `metrics_weighted`, no `stats`, every retrieved `(state, mode_id, generator)` row returned as-is, sorted by distance - shown to domain partners in this raw form so they can specify what aggregation (if any) they actually want; a fully-designed weighted alternative is parked in `~/.claude/plans/luminous-discovering-sparrow.md` for when that answer comes back. Not yet exposed via the API (service/domain layer only for now). `eles/2026-06` needed an extra fix: its raw SSSA files are keyed by `<YYYYMMDD>_<HHMM>` timestamp, not the `{batch}_{row}` state id LF/TSA/FSA use - `_load_sssa_state_mapping()` translates via `clean_files/Dates/Date_main_N.csv`; ~35/4282 SSSA timestamps have no corresponding LF row at all and are dropped (logged), not treated as an error. Generator names are intentionally left as their raw abbreviated/numeric-ID form (only lightly normalized, `NEK_-G1` -> `NEK-G1`) - no mapping to TSA/FSA's EIC codes, by deliberate decision (the partners who chose these names know what they map to; not this repo's concern to maintain that mapping).
- **Caveat, seam prepared but not yet filled in**: `ConAng`/`ObsAng`/`ParAng` (and their state-variable-suffixed variants, e.g. `ObsAng_speed`) are raw degree values with the same wraparound problem `phi_Bus*_[deg]` (LF) has, with no `AngleSinCos`-style sin/cos treatment. `EstimationService._transform_sssa_angles()` is a passthrough placeholder for that treatment, already wired into `estimate_sssa_by_generator()`'s per-neighbor metrics construction - currently the identity function, since nothing averages/compares these values yet (they pass through raw in the `metrics` dict). Fill in real angle encoding there once an actual aggregation is built, rather than adding a new seam at that point.
- Debug/data-science-oriented summary fields are now nested under a `stats` object (`Stats`/`ReportStats`/`LocationReportStats` in `src/domain/estimation/models.py`), separate from the primary user-facing value (`cct_weighted`/`cct_weighted_per_location`/`metrics_weighted`) on each summary.
- Callers can now choose how many neighbors to retrieve via `StateRequest.n_neighbors` (`src/api/estimate.py`), threaded through `_query_neighbors`/`db.query(limit=...)`; defaults to `DatabaseQdrant.default_limit` when unset.
- **Implemented 2026-07-25: multi-variant `topology_cols` for `eles/2026-06`** (production code done; the confidential benchmark rerun + paper update are separate, still-open follow-ups tracked in `paper/TODO.md`). `datasets/eles/2026-06/transform.py` now has `filter_topology_cols_full`/`_lines_only`/`_slovenia_only` and writes three `topology_cols_<variant>.json`/`.joblib.z` files (verified: 1072/907/254 columns respectively; `slovenia_only` matches `eles/2026-01`'s existing file exactly). `QdrantConfig.topology_variant` (env `TOPOLOGY_VARIANT`, default `lines_only`) is folded into `collection_name` for every dataset - not just `eles/2026-06` - since `DatabaseQdrant.fit(force=False)` only populates a collection that doesn't exist yet, so switching variants without renaming the collection would silently keep serving stale `topology_id` payloads from the old variant. `_dataset_paths()` gained an optional `topology_variant` kwarg (backward compatible - `bus39`/`interscada/*`/`eles/2026-01` fall back to their single unversioned `topology_cols.json` unchanged). 10 new tests in `tests/test_estimation_service.py`/`tests/test_qdrant_config.py`; full suite 54/54 passing. Below is the original investigation this implements: Investigation found the current single `topology_cols.json` is unusable either way - "full" (all `oserv_*`) fragments ~95% of records into singleton topology groups, while `eles/2026-01`'s dictionary-matched "slovenia-only" subset (254 cols) is constant across every record on the data available so far (collapses to 1 group - exact-topology retrieval is currently a no-op for `eles/2026-01`). **This reflects the current, still-growing data batch (ELES/partners still producing more simulation batches, slow to generate; no recorded Slovenian topology change yet), not a flaw in the dictionary-matching logic** (independently re-derived and confirmed correct) - re-evaluate "slovenia-only" once more diverse batches land. A "lines-only" definition (all `oserv_Lne*`/`oserv_Line*`, generators excluded - physically the right cut, since topology should mean network connectivity, not generator dispatch) gives a genuinely usable grouping on today's data (1,790 groups, 76% of records get >=1 same-topology neighbor). Full write-up, the empirical check (generator-off correlates 100% with exactly-zero power across all 165 generators checked - **flagged as possibly a partner-side export artifact for generators specifically, not confirmed as independent validation; the same reversed `oserv_*` encoding polarity is more reliably confirmed for lines/buses** via power-flow correlation, see the corrected comment at `service.py`'s `# oserv_ ~= in/out-of-service status flag`), and the implementation plan (three named variants: `full`/`lines-only`/`slovenia-only`, config-selectable, `lines-only` as the sane default) are in `datasets/eles/2026-06/README.md`'s "Topology Variants" section. Scope: `eles` only (`bus39`/`interscada/*` untouched), and use `eles/2026-06` not `2026-01` going forward. An exploratory (non-production) evaluation already ran against `eles/2026-01` via `scripts/service/eles_topology_candidate_eval.py` - needs re-running against `eles/2026-06` once the production variant-selection code lands, before any paper numbers are updated.
- **Investigated 2026-07-28: SSSA generator-set matching, ahead of finalizing the beta SSSA API endpoint** (investigation only, no production code changed - full write-up in `datasets/eles/2026-06/README.md`'s "SSSA Generator-Set Matching" section). Since `mode_id` isn't comparable across states (see the SSSA sections of `datasets/eles/*/README.md`), `estimate_sssa_by_generator()` groups by `generator` instead - this checked whether that's actually a stable matching key, at two granularities, both using strict set-equality grouping (no subset/overlap credit, mirroring the topology bitstring's exact-match semantics above). State-vs-state (`scripts/service/eles_sssa_generator_set_eval.py`, grouping each state by the exact set of generators it reports SSSA for): `eles/2026-01` collapses to 86 distinct sets across all 4,402 states (every state has an exact-match twin, max group 149); `eles/2026-06` fragments to 4,212 distinct sets across 4,247 covered states (~98.4% of states have no exact-match twin, max group 3) - the same shape of outcome as the topology-column finding above. Within-state, across-mode (worked interactively in `reports/xx_sssa_analysis.ipynb`, grouping `(state, mode_id)` pairs): `eles/2026-01` states typically have only 1-8 distinct generator sets across all their modes, with a median within-state max-group-size of ~155 (out of up to ~181 modes) - i.e. nearly all of a state's modes agree; `eles/2026-06` states typically have ~13-15 distinct sets across their modes, with a median within-state max-group-size of just 1 (max observed anywhere: 3) - i.e. almost no two modes within the same state agree on which generators contributed. **Conclusion: exact generator-set matching is not usable for `eles/2026-06` at either granularity** - whatever the beta SSSA endpoint's contract ends up being, it cannot gate retrieval on exact generator-set identity for this dataset; a genuinely relaxed/overlap-based definition would be a materially different design, not decided here, and remains open pending domain input (same open question as the raw/unweighted `estimate_sssa_by_generator()` design above). `eles/2026-01`'s opposite (consistent) result is flagged as a likely artifact of that drop's lower export fidelity (per-batch, no state-variable breakdown) rather than evidence the matching problem is solved there.
